// nastepne kroki:

// channel class:

// add clients - avoid duplicates
// remove clients
// make a client an operator - elevate their privilages? They should be able to KICK other users, also INVITE, TOPIC, MODE
// check if client is an operator
// each Client should have a reference to the Channel (so we know which channel they are currently in)
// ???

